# Example: repo-history analyzing itself

This directory is real, unedited output from running `repo-history` on its own
repository at commit `210d2dc` (7 commits). It's here so you can see what the
tool produces without running it.

It was generated by the documented pipeline: `repo-history plan` →
the `/repo-history` skill (3 subagents, one per episode, plus a synthesis) →
`repo-history build`. Result: **3 episodes → 15 decisions, 5 landmines.**

| file | what it is |
| --- | --- |
| [`TIMELINE.md`](./TIMELINE.md) | How the codebase evolved, in chapters. |
| [`DECISIONS.md`](./DECISIONS.md) | Decisions inferred from history, with evidence commits. |
| [`LANDMINES.md`](./LANDMINES.md) | Do-not-repeat lessons. The highest-value artifact. |
| [`ARCHITECTURE.md`](./ARCHITECTURE.md) | The system today, and how it got here. |
| [`HOTSPOTS.md`](./HOTSPOTS.md) | Churn and change-coupling. Purely mechanical, zero LLM tokens. |

## Why this is the point

Every landmine below was reconstructed *from commit diffs alone* — none of it is
written down anywhere in the current source:

> **Do not parse git `--numstat` paths as plain strings; rename output has three
> distinct shapes.** git emits renames as `old.py => new.py`,
> `src/{old => new}/file.py`, and `src/{ => sub}/file.py` (empty left side) …
> naive splitting will silently produce bogus paths.

> **Never trust a recorded head sha blindly — check it still exists before
> computing a `since` range.** After a rebase or force-push the head stored in
> `index.json` can vanish from the repo … rather than silently producing a wrong
> (and wrongly-scoped) commit range.

> **Do not compute file change-coupling over huge commits; skip anything touching
> more than ~40 files.** Initial imports and mass reformats are structural churn.
> Counting every file pair in them is quadratic and produces couplings that mean
> nothing.

That is the tacit knowledge a senior engineer carries and a new agent doesn't.

> **Note:** this is a point-in-time snapshot and is not regenerated on every
> commit, so it will drift from the current source. Run the tool yourself for
> up-to-date memory.
